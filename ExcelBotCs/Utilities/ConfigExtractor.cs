namespace ExcelBotCs.Utilities;

public class ConfigExtractor
{
    // Get config value from environment variables, if not existend, use config value from appsettings.json
    // After that, maybe default or throw exception
}